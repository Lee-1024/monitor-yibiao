# 显存仪表调试

接第三个 MCP4725 到 PCA 的 CH2：

```text
PCA SD2 -> 显存 MCP4725 SDA
PCA SC2 -> 显存 MCP4725 SCL
3V3 -> MCP VCC
公共 GND -> MCP 两个 GND
MCP OUT -> 显存电流表正端
显存表负端 -> 公共 GND
```

Go 配置：

```json
{"test_mode":true,"test_cpu":0,"test_memory":0,"test_gpu":50}
```

重新启动 Go 后，ESP32 串口应显示 `MCP 0x60 detected on CH2` 和对应的 active channel mask。将 `test_gpu` 依次设置为 0、25、50、75、90、100，逐点观察显存表指针。显存表应独立校准；如果 100% 不能到满刻度，记录实际刻度并按比例调整 CH2 的满量程码值。
