# ESP32 监控仪表接线图（新手版）

## 0. 先确认

- ESP32：`Espressif ESP32 Dev Module`。
- PCA9548A 电源脚：`VIN`，支持 2.3–5.5V。
- MCP4725 电源脚：`VCC`。
- 三只表：每只约 1.5kΩ，满量程 100uA。
- 不使用恒流模块，MCP4725 的 `VOUT` 直接接表头。

## 1. ESP32 到 PCA9548A

| ESP32 | PCA9548A | 作用 |
|---|---|---|
| `3V3` | `VIN` | 3.3V 电源 |
| `GND` | `GND` | 地 |
| `GPIO21` | `SDA` | I2C 数据 |
| `GPIO22` | `SCL` | I2C 时钟 |

PCA9548A 的 `A0`、`A1`、`A2` 全部接 `GND`，地址为 `0x70`。

## 2. PCA9548A 到三个 MCP4725

| 用途 | PCA 通道 | SDA 线 | SCL 线 |
|---|---|---|---|
| CPU 表 | CH0 | `SD0` -> CPU 模块 `SDA` | `SC0` -> CPU 模块 `SCL` |
| 内存表 | CH1 | `SD1` -> 内存模块 `SDA` | `SC1` -> 内存模块 `SCL` |
| 显存表 | CH2 | `SD2` -> 显存模块 `SDA` | `SC2` -> 显存模块 `SCL` |

每个 MCP4725 的 `VCC` 接 ESP32 `3V3`，`A0` 接 `GND`。三个模块地址都为 `0x60`，因位于不同 PCA 通道不会冲突。

## 3. MCP4725 的两个 GND

两个 `GND` 通常是同一地（电源地和模拟地）。**两个 GND 都接 ESP32 的 GND**，不能接 3.3V，也不要悬空；若具体模块说明书另有要求，以说明书为准。

## 4. MCP4725 到电流表

```text
CPU MCP4725 VOUT  -> CPU 电流表正端（+）
内存 MCP4725 VOUT -> 内存电流表正端（+）
显存 MCP4725 VOUT -> 显存电流表正端（+）
三只电流表负端（-） -> ESP32 GND
```

不要把表头正端接到 `3V3` 或 `VIN`。软件把 VOUT 限制在约 `0–0.15V`，因为 `100uA × 1.5kΩ ≈ 0.15V`。

## 5. 上电前检查

1. `PCA VIN`、三个 `MCP VCC` 都接 `ESP32 3V3`。
2. 所有 `GND` 共地，两个 MCP GND 都已接地。
3. CH0、CH1、CH2 的 SDA/SCL 没有交叉。
4. PCA 的 A0/A1/A2 和 MCP 的 A0 都接 GND。
5. 上电后软件先把三个 DAC 写成 0。
