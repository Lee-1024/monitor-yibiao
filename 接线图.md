# 按实物丝印接线（PCA9548A + 3 个 MCP4725）

## 一、ESP32 到 PCA9548A 左侧

| ESP32 引脚 | PCA9548A 实物引脚 | 说明 |
|---|---|---|
| `3V3` | `VIN` | 电源；你的模块 VIN 支持 2.3–5.5V |
| `GND` | `GND` | 公共地 |
| `GPIO21` | `SDA` | I2C 数据 |
| `GPIO22` | `SCL` | I2C 时钟 |
| `3V3` | `RST` | 保持复位脚为高电平 |
| `GND` | `A0` | 地址选择 |
| `GND` | `A1` | 地址选择 |
| `GND` | `A2` | 地址选择 |

PCA9548A 地址：`0x70`。

注意：表中 `RST` 应接 `3V3`，不是 GND。若你的模块已有上拉电阻，RST 可以不接；为了明确，建议直接 `RST -> 3V3`。

## 二、PCA9548A 到 MCP4725（按照片上的丝印）

MCP4725 模块丝印从上到下是：`OUT、GND、SCL、SDA、VCC、GND`。

### MCP4725-CPU（接 PCA 的 CH0）

| PCA9548A 实物引脚 | MCP4725-CPU 实物引脚 |
|---|---|
| `SD0` | `SDA` |
| `SC0` | `SCL` |
| `VIN`（或 ESP32 `3V3`） | `VCC` |
| `GND` | 上面的 `GND` |
| `GND` | 下面的 `GND` |
| `A0` | `GND` |

### MCP4725-内存（接 PCA 的 CH1）

| PCA9548A 实物引脚 | MCP4725-内存 实物引脚 |
|---|---|
| `SD1` | `SDA` |
| `SC1` | `SCL` |
| `VIN`（或 ESP32 `3V3`） | `VCC` |
| `GND` | 上面的 `GND` |
| `GND` | 下面的 `GND` |
| `A0` | `GND` |

### MCP4725-显存（接 PCA 的 CH2）

| PCA9548A 实物引脚 | MCP4725-显存 实物引脚 |
|---|---|
| `SD2` | `SDA` |
| `SC2` | `SCL` |
| `VIN`（或 ESP32 `3V3`） | `VCC` |
| `GND` | 上面的 `GND` |
| `GND` | 下面的 `GND` |
| `A0` | `GND` |

三个 MCP4725 的地址都为 `0x60`，因为分别在 CH0、CH1、CH2，不会冲突。

## 三、MCP4725 到电流表

| MCP4725 实物引脚 | 电流表 |
|---|---|
| CPU 模块 `OUT` | CPU 表正端 `+` |
| 内存模块 `OUT` | 内存表正端 `+` |
| 显存模块 `OUT` | 显存表正端 `+` |
| 三只电流表负端 `-` | ESP32 `GND` |

## 四、MCP4725 两个 GND 是否一样

通常是的，两个 `GND` 都是地，模块上一般分别引出数字地和模拟地。这里两个都接 ESP32 的 `GND`，不要一个接 3.3V。若只想接一根，至少接靠近 `VCC` 的那个 GND；但本项目建议两个都接。

## 五、最终只需要记住的线

```text
ESP32 3V3   -> PCA VIN
ESP32 GND   -> PCA GND
ESP32 GPIO21 -> PCA SDA
ESP32 GPIO22 -> PCA SCL
ESP32 3V3   -> PCA RST
ESP32 GND   -> PCA A0/A1/A2

PCA SD0/SC0 -> CPU MCP SDA/SCL
PCA SD1/SC1 -> 内存 MCP SDA/SCL
PCA SD2/SC2 -> 显存 MCP SDA/SCL

ESP32 3V3 -> 三个 MCP VCC
ESP32 GND -> 三个 MCP 的两个 GND
三个 MCP A0 -> GND
三个 MCP OUT -> 对应电流表正端
三只电流表负端 -> ESP32 GND
```
