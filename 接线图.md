# ESP32 + PCA9548A + 3x MCP4725 + 3x100uA 电流表接线图

## 1. 系统连接总图

```text
┌──────────────────────┐       Wi‑Fi        ┌─────────────────────────────┐
│ PC / Go采集程序       │ ─────────────────> │ ESP32 DevKit                 │
│ CPU/内存/显存百分比   │                    │ 3.3V逻辑，独立5V供电          │
└──────────────────────┘                    │                             │
                                            │ GPIO21 SDA ─────┐            │
                                            │ GPIO22 SCL ─────┼──> PCA9548A │
                                            │ 3V3 ────────────┤            │
                                            │ GND ────────────┘            │
                                            └─────────────────────────────┘
                                                          │
                         I2C下游通道                      │
       ┌─────────────────┼─────────────────┐              │
       ▼                 ▼                 ▼              │
  PCA CH0           PCA CH1           PCA CH2              │
  MCP4725-CPU       MCP4725-MEM       MCP4725-GPU           │
       │                 │                 │                │
       ▼                 ▼                 ▼                │
  直接连接-CPU       直接连接-MEM       直接连接-GPU          │
       │                 │                 │                │
       ▼                 ▼                 ▼                │
  100uA电流表       100uA电流表       100uA电流表            │
```

## 2. ESP32 引脚

| ESP32 引脚 | 连接 | 说明 |
|---|---|---|
| GPIO21 | PCA9548A SDA | I2C 数据 |
| GPIO22 | PCA9548A SCL | I2C 时钟 |
| 3V3 | PCA9548A VIN、三个 MCP4725 VCC | 3.3V；PCA9548A VIN 支持 2.3–5.5V |
| GND | PCA9548A GND、三个 MCP4725 GND、模拟驱动地 | 共地，单点汇接 |
| EN/RESET | 10kΩ 上拉至 3V3 | 可选，按开发板默认连接 |

不要使用 GPIO34–39 作为 I2C。DAC 输出限制在 0–0.15V 后直接接电流表。

## 3. PCA9548A 地址与通道

```text
PCA9548A A0 = GND
PCA9548A A1 = GND
PCA9548A A2 = GND
主地址 = 0x70
```

| PCA9548A 通道 | 下游 SDA/SCL | MCP4725 A0 | MCP4725 地址 | 指标 |
|---|---|---|---|---|
| CH0 | SD0/SC0 | GND | 0x60 | CPU |
| CH1 | SD1/SC1 | GND | 0x60 | 内存 |
| CH2 | SD2/SC2 | GND | 0x60 | 显存 |

三个 MCP4725 可以使用相同地址，因为 PCA9548A 将它们隔离在不同总线通道。

## 4. 每个 MCP4725 的接线

```text
MCP4725 VCC  ───── 3V3
MCP4725 GND  ───── GND
MCP4725 SDA  ───── 对应 PCA9548A SDx
MCP4725 SCL  ───── 对应 PCA9548A SCx
MCP4725 A0   ───── GND
MCP4725 VOUT ───── 对应电流表正端
```

每个模块 VCC/VIN-GND 旁放置 0.1uF 去耦电容；PCA9548A 上游 SDA/SCL 各使用 4.7kΩ 上拉至 3V3。若模块已带上拉，整条总线只保留一组有效上拉。

## 5. DAC 与电流表直连

```text
MCP4725 VOUT ──────────> 电流表正端
电流表负端 ────────────> GND
```

表头内阻约 1.5kΩ，100uA 满量程需要约 0.15V。软件必须设置 DAC 码值硬上限，异常和掉线时输出零值。

## 6. 电源

```text
USB/5V独立电源 ──> ESP32 VIN/5V
同一电源经低噪声3.3V稳压 ──> ESP32 3V3、PCA9548A VIN、MCP4725 VCC
模拟恒流驱动 ──> 按运放/晶体管规格接 5V 或独立模拟电源
所有 GND ───────> 单点汇接
```

上电时软件先将三个 DAC 写入 0，恒流驱动输出应处于零电流安全状态。
