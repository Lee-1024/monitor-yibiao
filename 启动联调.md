# Go 与 ESP32 首次联调（CPU 输出）

PlatformIO 扩展安装的 pio 可能不在 PATH。使用绝对路径：

```bash
PIO="$HOME/.platformio/penv/bin/pio"
cd /Users/lijinghua/Documents/monitor-yibiao/esp32
"$PIO" run
"$PIO" run -t upload
"$PIO" device monitor -b 115200
```

macOS 打包 Go 端：在项目根目录执行 `chmod +x build-macos.sh && ./build-macos.sh`，然后进入 `dist/monitor-sender-macos`，复制 `config.example.json` 为 `config.json` 并运行 `./monitor-sender`。

## 烧录 ESP32

先修改 `esp32/src/main.c` 顶部的 `WIFI_SSID` 和 `WIFI_PASSWORD`，然后：

```bash
cd esp32
pio run
pio run -t upload
pio device monitor -b 115200
```

看到 `started` 后，ESP32 已监听 UDP `9000`。

## 启动 Go 端

把 `monitor-sender.exe` 放到单独目录，第一次双击会生成 `config.json` 并退出。编辑为：

```json
{
  "esp32_address": "ESP32_IP:9000",
  "interval_ms": 200,
  "test_mode": true,
  "test_cpu": 50
}
```

替换 `ESP32_IP` 后再次双击 exe。日志在 `monitor-sender.log`。

## 万用表

断开电流表，黑表笔接公共 GND，红表笔接 CPU 对应 MCP4725 的 `OUT`。修改 `test_cpu` 为 0、25、50、75、100，理论电压分别为 0、0.0375、0.075、0.1125、0.15V。确认后将 `test_mode` 改为 `false`。
