# CPU/内存/显存模拟电流表监控系统设计文档

## 1. 总体架构
系统分为数据源、Wi‑Fi 接入、协议解析、指标处理、DAC 输出和诊断配置六层。数据源只需实现抽象发送接口，ESP32 不依赖具体操作系统或采集工具。

```text
数据源 -> Wi‑Fi -> ESP32网络任务 -> 协议校验 -> 指标处理/平滑 -> DAC服务
                                                        |-> PCA9548A -> 3x MCP4725 -> 100uA电流表
```

## 2. 硬件设计
### 2.1 I2C
ESP32 SDA/SCL 连接 PCA9548A 上游；PCA9548A 的三个下游端口各连接一个 MCP4725。每次写 DAC 前选择端口，完成写入后可关闭通道。所有器件共地，I2C 上拉电阻按总线电容计算，建议 2.2–4.7 kΩ、3.3 V。

### 2.2 模拟输出
MCP4725 VOUT 直接连接约 1.5 kΩ 表头，表头另一端接 GND。100uA 满量程需要约 0.15V；3.3V 参考时 DAC 码值上限约为 186。软件必须硬限制输出，建议校准满量程为 0.135–0.145V。

### 2.3 供电与保护
ESP32 与模拟级使用独立稳压电源；模拟地和数字地采用单点汇接。加入反接、过流、ESD 和电源去耦。电流表回路串联可测量电阻或测试点，便于校准。

## 3. 软件模块
- `wifi_manager`：配网、连接、重连、RSSI。
- `transport`：UDP/TCP 抽象接口、接收缓冲和心跳。
- `protocol`：帧解析、版本检查、CRC/长度校验、序号去重。
- `metrics`：范围钳位、平滑、超时判定、映射。
- `dac_manager`：PCA9548A 选通、MCP4725 写入、重试和错误统计。
- `calibration`：零点/满量程参数、NVS 读写和版本迁移。
- `diagnostics`：状态机、日志、看门狗和安全输出。

### 3.1 PC 采集端平台策略
- Apple Silicon Mac：使用 GPU 活跃度和统一内存统计。
- Windows：存在独立显卡时只选择独立显卡，否则选择集成显卡。
- Linux：存在独立显卡时只选择独立显卡，否则选择集成显卡。
- 三个平台均输出统一的 `gpu` 百分比，ESP32 不感知数据来源。

## 4. 数据协议
推荐 JSON（便于调试）或紧凑二进制（量产）。JSON 示例：

```json
{"v":1,"seq":42,"ts":1720000000,"cpu":37.5,"mem":62.0,"gpu":18.2}
```

服务端应校验字段存在性、数值有限性和范围；可选增加 HMAC 或局域网访问控制。二进制协议应包含魔数、版本、长度、序号、三个 uint16 百分比（0.01% 单位）和 CRC16。

## 5. 核心算法
对每个指标先钳位到 `[0,100]`，再执行一阶 IIR：`y = y + alpha * (x-y)`；`alpha` 根据采样周期和配置时间常数计算。DAC 码值：`code = clamp((y * scale + offset), 0, 4095)`。写入顺序为选择通道、写 DAC、检查 ACK、记录结果；单通道失败不影响其他通道。

## 6. 任务与状态机
- 网络任务：接收并校验数据，写入线程安全队列。
- 控制任务：50 Hz 取最新指标、平滑、更新三个 DAC。
- 诊断任务：1 Hz 检查超时、Wi‑Fi、I2C 错误和喂狗。
- 状态迁移：BOOT→CONNECTING→ONLINE；ONLINE 超时→STALE；网络恢复→ONLINE；硬件错误→ERROR。STALE/ERROR 输出安全零值或配置的保持值。

## 7. 校准流程
装配后先断开输入并调整零点，再施加 100% 指令调整满量程；记录实际 DAC 码值，计算每通道 `scale/offset` 写入 NVS。校准命令需回读并校验，参数带版本和 CRC。

## 8. 测试计划
单元测试覆盖协议边界、CRC、钳位、平滑和码值计算；硬件测试覆盖 I2C 多路切换、DAC 断线、过流保护和五点线性；系统测试覆盖断网/重连、非法帧、看门狗和 24 小时稳定性。验收数据应记录实际电流、指针位置和错误计数。

## 9. 风险与决策
最大风险是 DAC 电压到 100 uA 的模拟驱动误差和 MCP4725 地址冲突。通过 PCA9548A 隔离地址、硬件限流、逐通道校准和测试点降低风险。Wi‑Fi UDP 延迟低但可能丢包，协议序号与超时策略保证显示安全。
