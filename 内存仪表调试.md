# 内存仪表调试

接第二个 MCP4725 到 PCA 的 CH1：

```text
PCA SD1 -> 内存 MCP4725 SDA
PCA SC1 -> 内存 MCP4725 SCL
3V3 -> MCP VCC
公共 GND -> MCP 两个 GND
MCP OUT -> 内存表正端
内存表负端 -> 公共 GND
```

Go 配置：

```json
{"test_mode":true,"test_cpu":0,"test_memory":50}
```

重新启动 Go 后确认串口有 `MCP 0x60 detected on CH1` 和 `active DAC channel mask: 0x03`。CPU 表约 3kΩ、内存表约 5kΩ，必须独立校准；当前 CH0 满量程码值为 390，CH1 校准值为 637。将 `test_memory` 设置为 0、25、50、75、90、100 逐点测量。100% 测试时确认电流不超过 100uA，若超过立即停止并降低 CH1 码值。
