# CPU/内存/显存模拟电流表监控系统需求文档

## 1. 项目概述
本项目使用 ESP32 控制三路最大量程 100 uA 的直流电流表，以指针位置模拟显示 CPU、内存、显存使用率。系统通过 Wi‑Fi 接收外部数据源发送的监控指标，经 PCA9548A I2C 多路复用器连接三个 MCP4725 DAC，输出对应模拟电压/电流驱动电流表。

## 2. 目标与范围
- 实时显示 0–100% 的 CPU、内存、显存使用率。
- 支持外部采集端通过定义好的 Wi‑Fi 协议发送数据；采集端实现不限定。
- 提供启动、联网、数据超时、故障和校准状态。
- 本期不负责 PC 指标采集、不实现历史曲线、不接入云服务。

## 3. 功能需求
### 3.1 通信
- ESP32 作为 Wi‑Fi Station，支持配置 SSID、密码和设备名。
- 提供 UDP（低延迟）或 TCP（可靠）抽象传输层；默认采用 UDP，应用层带序号和校验。
- 数据帧包含协议版本、序号、时间戳、CPU/内存/显存百分比及可选温度字段。
- 接收合法帧后在 1 秒内更新显示；连续 3 秒未收到合法帧进入超时状态。

### 3.2 指标与显示
- 三路默认映射：DAC0→CPU，DAC1→内存，DAC2→显存。
- 输入范围 0–100%，超范围值钳位；支持小数百分比。
- 显示值采用可配置的一阶平滑，默认时间常数 300 ms，减少指针抖动。
- 0% 对应电流表零位，100% 对应满量程 100 uA；映射和反向方向可配置。

### 3.3 DAC 与电流表
- 通过 PCA9548A 选择三个 MCP4725 通道，避免地址冲突。
- MCP4725 使用 12 位输出；输出更新频率不低于 10 Hz，目标 50 Hz。
- 不增加恒流驱动级，MCP4725 直接驱动实测约 2 kΩ 表头；软件将输出限制在约 0–0.20V。
- 上电默认输出安全零电流；异常、超时或看门狗复位时关闭或降至安全电平。

### 3.4 校准与配置
- 支持零点、满量程校准，保存至 NVS，并可恢复默认值。
- 支持设置 Wi‑Fi、传输模式、指标映射、平滑参数、DAC 增益/偏置。
- 提供配置入口（推荐串口命令或临时 SoftAP 网页）。

### 3.5 状态与诊断
- 状态包括 BOOT、CONNECTING、ONLINE、STALE、ERROR、CALIBRATION。
- 提供运行日志、RSSI、最后数据时间、I2C/DAC 错误计数。
- Wi‑Fi 断线自动重连，指数退避，最长重试间隔 60 秒。

## 4. 非功能需求
- ESP32 主循环不被网络阻塞；关键任务使用 FreeRTOS 独立任务。
- 正常运行功耗由独立电源设计确定，连续运行不少于 24 小时。
- 电流输出误差目标：满量程 ±2%（校准后）；通道间一致性 ±1%FS。
- 输入数据非法率达到 100% 时不得导致复位或危险输出。
- 代码、协议和校准数据版本化，支持 OTA 预留接口。

## 5. 验收标准
在 Wi‑Fi 正常、输入 0/25/50/75/100% 五个点下，三表指针均正确响应；断网 3 秒内进入超时安全状态；恢复数据后 1 秒内恢复显示；连续运行 24 小时无死机，满量程电流不超过 100 uA。

## 6. 待确认项
电流驱动拓扑和参考电压、PCA9548A 地址、MCP4725 地址/通道分配、三表最终指标映射、Wi‑Fi 配网方式及 UDP/TCP 最终选择。
